\documentclass[12pt, a4paper]{report}

% 

\usepackage{elegant-cours}
\usepackage{tikz}
\usetikzlibrary{matrix}
\makeindex

\begin{document}

\MaPageDeGarde{Chapitre XIV-C : Matrices}{Retranscrit par Samy Youssoufine}{./assets/logo.png}{WIP. Peut contenir des erreurs ou des sections incomplètes.}

\tableofcontents
\clearpage

\vspace*{\fill}
Ce chapitre est consacré aux matrices, qui sont des outils fondamentaux en algèbre linéaire. Nous allons introduire les définitions de base, les propriétés essentielles, et quelques applications importantes des matrices dans le contexte de l'algèbre linéaire.
\vspace*{\fill}

\chapter{Définitions}

\begin{importantbox}
    $\Kset$ désigne un corps commutatif dans le reste de cette section.
\end{importantbox}

\begin{definition}[Matrice]
    Soient $n,p \in \N^*$.
    \begin{enumerate}
        \item On appelle matrice à $n$ lignes et $p$ colonnes toute application :
        $$A : \begin{matrix}
        [\![1,n]\!] \times [\![1,p]\!] &\to &\Kset
        \\ (i,j) &\mapsto &A(i,j) \overset{\text{noté}}= a_{i,j}
        \end{matrix}$$
        qu'on note aussi $A = (a_{i,j})_{\substack{1 \leq i \leq n\\ 1\leq j \leq p}}$.
        \begin{itemize}
            \item On peut écrire $\left(\begin{matrix}
            a_{1,1} & \cdots & a_{1,p}
            \\
            a_{2,1} & \cdots & a_{2,p}
            \\
            \vdots & & \vdots
            \\
            a_{n,1} & \cdots & a_{n,p}
            \end{matrix}\right)$
            \item $a_{i,j}$ est appelé le coefficient de $A$ situé à la ligne $i$ et à la colonne $j$.
            \item $(n,p)$ est appelé la \textit{taille} de $A$.
        \end{itemize}
        \item On note $\mathcal M_{n,p}(\Kset)$ l'ensemble de toutes les matrices à $n$ lignes et $p$ colonnes à coefficients dans $\Kset$.
        \item Si $n=p$, on dit que $A$ est une matrice carrée de taille $n$. On note $\mathcal M_n(\Kset) = \mathcal M_{n,n}(\Kset)$ l'ensemble des matrices carrées de taille $n$ à coefficients dans $\Kset$.
    \end{enumerate}
\end{definition}

\begin{remark}
    \begin{enumerate}
        \item $\mathcal M_{n,1}(\Kset)$ est l'ensemble des matrices ``colonnes'' à $n$ lignes.
        \item $\mathcal M_{1,p}(\Kset)$ est l'ensemble des matrices ``lignes'' à $p$ colonnes.
    \end{enumerate}
    \textit{On ne peut pas encore les identifier à des éléments de $\Kset^n$ ou $\Kset^p$. Pour cela, on doit d'abord définir un isomorphisme entre ces ensembles.}
\end{remark}

\begin{example}
    \begin{outline}
        \1 Soient $\lambda_1,\dots,\lambda_n \in \Kset$. On pose $V = \left(\lambda_{i}^{j-1}\right)_{\substack{1 \leq i \leq n\\ 1\leq j \leq n}}$. $V$ est appelée la matrice de Vandermonde\index{Vandermonde (Matrice de)} associée à $\lambda_1,\dots,\lambda_n$. Cette matrice est de taille $n$.
        \2 On peut expliciter $V$ :
        $$V = \left(\begin{matrix}
1 & \lambda_1 & \lambda_1^2 & \cdots & \lambda_1^{n-1}
\\
1 & \lambda_2 & \lambda_2^2 & \cdots & \lambda_2^{n-1}
\\
\vdots & \vdots & \vdots & & \vdots
\\
1 & \lambda_n & \lambda_n^2 & \cdots & \lambda_n^{n-1}
\end{matrix}\right)$$
        \1 Pour tout $(s,q) \in [\![1,n]\!] \times [\![1,p]\!]$, on a $E_{s,q} = (\delta_{s,i}\cdot \delta_{q,j})_{\substack{1 \leq i \leq n\\ 1\leq j \leq p}}$. $E_{s,q}(n,p)$ est appelée la matrice élémentaire\index{Élémentaire (Matrice)} de taille $n$ associée à $(s,q)$.
            \2 On peut expliciter $E_{s,q}(n,p)$ :
            $$E_{s,q}(n,p) = \left(\begin{matrix}
\delta_{s,1}\delta_{q,1} & \delta_{s,1}\delta_{q,2} & \cdots & \delta_{s,1}\delta_{q,p}
\\
\delta_{s,2}\delta_{q,1} & \delta_{s,2}\delta_{q,2} & \cdots & \delta_{s,2}\delta_{q,p}
\\
\vdots & \vdots & & \vdots
\\
\delta_{s,s}\delta_{q,1} & \delta_{s,s}\delta_{q,2} & \cdots & \delta_{s,s}\delta_{q,p}
\end{matrix}\right)$$
            \2 Ce qui donne $E_{s,q}(n,p) = \left(\begin{matrix}
0 & \cdots & 0 & \cdots & 0
\\
\vdots & & \vdots & & \vdots
\\
0 & \cdots & 1 & \cdots & 0
\\
\vdots & & \vdots & & \vdots
\\
0 & \cdots & 0 & \cdots & 0
\end{matrix}\right)_{(n,p)}$ ($1$ est en ligne $s$, col. $q$)
    \2 La famille $(E_{s,q}(n,p))_{\substack{1 \leq s \leq n\\ 1\leq q \leq p}}$ est appelée la famille des matrices élémentaires de $\mathcal M_{n,p}(\Kset)$.
    \end{outline}
\end{example}

\begin{definition}[Matrice triangulaire et diagonale]
    Soit $A = (a_{i,j})_{\substack{1 \leq i \leq n\\ 1\leq j \leq n}} \in \mathcal M_{n}(\Kset)$.

    \begin{outline}[enumerate]
        \1 Les $(a_{1,1},\dots,a_{n,n})$ sont appelés les coefficients diagonaux de $A$.
        \1 $A$ est dite triangulaire supérieure si $a_{i,j} = 0$ pour tout $1 \leq j < i \leq n$.
        \begin{itemize}
            \item On note $T_{n,s}(\Kset)$ l'ensemble des matrices triangulaires supérieures de taille $n$ à coefficients dans $\Kset$.
        \end{itemize}
        \1 $A$ est dite triangulaire inférieure si $a_{i,j} = 0$ pour tout $1 \leq i < j \leq n$.
        \begin{itemize}
            \item On note $T_{n,i}(\Kset)$ l'ensemble des matrices triangulaires inférieures de taille $n$ à coefficients dans $\Kset$.
        \end{itemize}
        \1 $A$ est dite diagonale si $a_{i,j} = 0$ pour tout $1 \leq i \neq j \leq n$. C'est-à-dire que $A$ est à la fois triangulaire supérieure et triangulaire inférieure.
        \begin{itemize}
            \item On note $\diag(a_{1,1},\dots,a_{n,n})$ la matrice diagonale de taille $n$ ayant pour coefficients diagonaux $a_{1,1},\dots,a_{n,n}$.
            \item On note $D_n(\Kset)$ l'ensemble des matrices diagonales de taille $n$ à coefficients dans $\Kset$.
        \end{itemize}
    \end{outline}
\end{definition}

\begin{figure}[h]
    \centering
    \begin{tikzpicture}
        \matrix[matrix of math nodes,left delimiter=(,right delimiter=),
        nodes in empty cells, row sep=2pt, column sep=4pt] (U) {
            a_{11} & a_{12} & a_{13} & a_{14} \\
            0 & a_{22} & a_{23} & a_{24} \\
            0 & 0 & a_{33} & a_{34} \\
            0 & 0 & 0 & a_{44} \\
        };
        \node[below=6pt of U] {Triangulaire superieure};
    \end{tikzpicture}
    \hspace{1.5cm}
    \begin{tikzpicture}
        \matrix[matrix of math nodes,left delimiter=(,right delimiter=),
        nodes in empty cells, row sep=2pt, column sep=4pt] (L) {
            a_{11} & 0 & 0 & 0 \\
            a_{21} & a_{22} & 0 & 0 \\
            a_{31} & a_{32} & a_{33} & 0 \\
            a_{41} & a_{42} & a_{43} & a_{44} \\
        };
        \node[below=6pt of L] {Triangulaire inferieure};
    \end{tikzpicture}
    \\
    \vspace{0.4cm}
    \begin{tikzpicture}
        \matrix[matrix of math nodes,left delimiter=(,right delimiter=),
        nodes in empty cells, row sep=2pt, column sep=4pt] (D) {
            a_{11} & 0 & 0 & 0 \\
            0 & a_{22} & 0 & 0 \\
            0 & 0 & a_{33} & 0 \\
            0 & 0 & 0 & a_{44} \\
        };
        \node[below=6pt of D] {Diagonale};
    \end{tikzpicture}
    \caption{Representations schematiques des matrices triangulaires et diagonales.}
\end{figure}


\begin{attention}
    Les matrices triangulaires n'imposent aucune condition sur les coefficients inférieurs/supérieurs à la diagonale. Ils peuvent aussi être nuls. Dans une matrice diagonale, les coefficients diagonaux peuvent aussi être nuls. La matrice nulle est, par exemple, à la fois triangulaire supérieure, triangulaire inférieure et diagonale.
\end{attention}

\begin{remark}
    L'intersection de $T_{n,s}(\Kset)$ et $T_{n,i}(\Kset)$ est exactement $D_n(\Kset)$.
    $$T_{n,s}(\Kset) \cap T_{n,i}(\Kset) = D_n(\Kset)$$
\end{remark}

\begin{definition}[Transposée d'une matrice]
    Soit $A = (a_{i,j})_{\substack{1 \leq i \leq n\\ 1\leq j \leq p}} \in \mathcal M_{n,p}(\Kset)$.
    
    La transposée de $A$ est la matrice $A^T = (a_{j,i})_{\substack{1 \leq j \leq p\\ 1\leq i \leq n}} \in \mathcal M_{p,n}(\Kset)$.

    On note cette transposée $A^T$ ou $^tA$.
\end{definition}

\begin{remark}
    Soit $A = (a_{i,j})_{\substack{1 \leq i \leq n\\ 1\leq j \leq p}} \in \mathcal M_{n,p}(\Kset)$.
    
    % on veut recréer cette notation
    %     L_1 (------)
    %     L_2 |------|
    % A = L_3 |------|
    %     L_4 |------|
    %     L_5 (------)

    On peut aussi noter $A$ de la manière suivante
    
        $$A = \begin{matrix}
            L_1 \\
            L_2 \\
            L_3 \\
            \vdots \\
            L_n
        \end{matrix}
        \overset{\begin{matrix} C_1 & C_2 & \cdots & C_p \end{matrix}}{\left(\begin{matrix}
        a_{1,1} & a_{1,2} & \cdots & a_{1,p}
        \\
        a_{2,1} & a_{2,2} & \cdots & a_{2,p}
        \\
        a_{3,1} & a_{3,2} & \cdots & a_{3,p}
        \\
        \vdots & \vdots & & \vdots
        \\
        a_{n,1} & a_{n,2} & \cdots & a_{n,p}
        \end{matrix}\right)}$$

    où $L_i = (a_{i,1},\dots,a_{i,p})$ est la $i$-ème ligne de $A$ pour tout $1 \leq i \leq n$.

    et où $C_i = \left(\begin{matrix} a_{1,i} \\ \vdots \\ a_{n,i} \end{matrix}\right)$ est la $i$-ème colonne de $A$ pour tout $1 \leq i \leq p$.

    Donc $^tL_i = \left(\begin{matrix} a_{i,1} \\ \vdots \\ a_{i,p} \end{matrix}\right)$ et $^tC_i = (a_{1,i},\dots,a_{n,i})$.
\end{remark}

\begin{attention}
    La transposée n'est pas une rotation de la matrice. Dans le cas d'une matrice carrée (lignes = colonnes), c'est une symétrie par rapport à la diagonale.
\end{attention}

\begin{example}
    \begin{outline}
        \1 Soit $A = \left(\begin{matrix}
            1 & 2 & 3
            \\
            4 & 5 & 6
        \end{matrix}\right)$. Alors $^tA = \left(\begin{matrix}
            1 & 4
            \\
            2 & 5
            \\
            3 & 6
        \end{matrix}\right)$.
        \1 Soit $A = \left(\begin{matrix}
            1 & 0 & 0
            \\
            i & 2 & 0
            \\
            0 & j & 3
        \end{matrix}\right)$. Alors $^tA = \left(\begin{matrix}
            1 & i & 0
            \\
            0 & 2 & j
            \\
            0 & 0 & 3
        \end{matrix}\right)$.
    \end{outline}
\end{example}

\begin{remark}
    Soit $A = (a_{i,j})_{\substack{1 \leq i \leq n\\ 1\leq j \leq p}} \in \mathcal M_{n,p}(\Kset)$.
    
    \begin{outline}[enumerate]
        \1 $\forall A \in D_n(\Kset), ^tA = A$.
        \1 $\forall A \in \mathcal M_{n,p}(\Kset), ^t(^tA) = A$.
        \1 L'application $T : \begin{matrix} \mathcal M_{n}(\Kset) &\to &\mathcal M_{n}(\Kset) \\ A &\mapsto &^tA \end{matrix}$ vérifie $T\circ T=\mathrm{Id}_{\mathcal M_n(\Kset)}$.
    \end{outline}
\end{remark}

\begin{definition}[Matrice symétrique et antisymétrique]
    Soit $A = (a_{i,j})_{1 \leq i, j \leq n} \in \mathcal M_{n}(\Kset)$.
    
    \begin{outline}
        \1 On dit que $A$ est symétrique si $^tA = A$. C'est-à-dire que $\forall 1 \leq i,j \leq n, a_{i,j} = a_{j,i}$.
            \2 \textit{Pour cela, $A$ doit être une matrice carrée.}
            \2 On note $S_n(\Kset)$ l'ensemble des matrices symétriques de taille $n$ à coefficients dans $\Kset$.
        \1 On dit que $A$ est antisymétrique si $^tA = -A$, donc que $\forall 1 \leq i,j \leq n, a_{i,j} = -a_{j,i}$.
            \2 On note $A_n(\Kset)$ l'ensemble des matrices antisymétriques de taille $n$ à coefficients dans $\Kset$.
    \end{outline}
\end{definition}

\begin{remark}
    Dans le cas d'une matrice antisymétrique, on a $a_{i,i} = -a_{i,i}$ pour tout $1 \leq i \leq n$, donc $2 a_{i,i} = 0$ pour tout $1 \leq i \leq n$. En particulier, si $\mathrm{car}(\Kset) \neq 2$, alors $a_{i,i} = 0$ pour tout $1 \leq i \leq n$. Donc la diagonale de la matrice antisymétrique est nulle.
\end{remark}

\begin{example}
    \begin{outline}
        \1 $A = \left(\begin{matrix}
            1 & i & 2 \\
            i & -2 & j \\
            2 & j & 0 \\
        \end{matrix}\right)$, est une matrice symétrique $\in S_3(\C)$.
        \1 $A = \left(\begin{matrix}
            0 & -1 & -2 & 1 \\
            1 & 0 & -3 & -1 \\
            2 & 3 & 1 & -4 \\
            -1 & 1 & 4 & 0 \\
        \end{matrix}\right)$, n'est pas une matrice antisymétrique, car $a_{3,3} = 1 \neq 0$. En le remplaçant par $0$, on obtient une matrice antisymétrique $\in A_4(\R)$.
    \end{outline}
\end{example}

\begin{definition}[Matrice de coordonnées]
    Soient $E$ un $\Kset$-ev. de dimension $n$ et $B = (e_1,\dots,e_n)$ une base de $E$.

    \begin{enumerate}
        \item $\forall x \in E, \exists ! (\lambda_1,\dots,\lambda_n) \in \Kset^n, x = \sum_{i=1}^n \lambda_i e_i$. 
        \\On note $\Mat_{B}(x) = \left(\begin{matrix}
            \lambda_1 \\ \vdots \\ \lambda_n
        \end{matrix}\right)$ la matrice de coordonnées de $x$ dans la base $B$.
        \item Soient $u_1,\dots,u_p \in E$. Pour tout $j \in \{1,\dots,p\}$, il existe d'uniques $(a_{i,j})_{1 \leq i \leq n} \in \Kset^n$ tels que $u_j = \sum_{i=1}^n a_{i,j} e_i$.\\On note $\Mat_B(u_1,\dots,u_p) = (a_{i,j})_{\substack{1 \leq i \leq n\\ 1\leq j \leq p}}$ la matrice de coordonnées de $(u_1,\dots,u_p)$ dans la base $B$.\\i.e. : $\Mat_B(u_1,\dots,u_p) = \left(\begin{matrix}
a_{1,1} & a_{1,2} & \cdots & a_{1,p}
\\ a_{2,1} & a_{2,2} & \cdots & a_{2,p}
\\ \vdots & \vdots & & \vdots
\\ a_{n,1} & a_{n,2} & \cdots & a_{n,p}
\end{matrix}\right)$
    \end{enumerate}
    
\end{definition}

\begin{example}
    Dans $\Kset_2[X]$, soient $P_1 = (X-1)^2$, $P_2 = X^2 - X$, $P_3 = 2X^2 - 1$ et $B = (1,X,X^2)$ la base canonique de $\Kset_2[X]$. Alors :
    $$\Mat_{B_{C}}(P_1,P_2,P_3) = \left(\begin{matrix}
        1 & 0 & -1 \\
        -2 & -1 & 0 \\
        1 & 1 & 2
    \end{matrix}\right)$$
\end{example}

\begin{definition}[Matrice d'une application linéaire]
    Soient $E,F$ deux $\Kset$-ev de dimensions respectives $n$ et $p$.
    
    Soient $B = (e_1,\dots,e_n)$ une base de $E$ et $C = (c_1,\dots,c_p)$ une base de $F$.

    Soit $f \in \mathcal L(E,F)$.

    On appelle matrice de $f$ relativement aux bases $B$ et $C$ la matrice notée $\Mat_{B,C}(f)$ ou $\Mat(f,B,C)$ égale à $\Mat_C(f(e_1),\dots,f(e_n)) \in \mathcal M_{n,p}(\Kset)$.
\end{definition}

\begin{remark}
    Si $f \in \mathcal L(E)$ et $B$ est une base de $E$, alors on écrit $\Mat(f,B,B)=[f]_B$. 
\end{remark}

\begin{example}
    \begin{outline}
        \1 Soit $f : \begin{matrix} \Kset_3[X] &\to &\Kset_3[X] \\ P &\mapsto &XP'+(X^2-1)P'' + 3P \end{matrix}$ qui associe à tout $P \in \Kset_3[X]$ l'image.\\On souhaite déterminer $[f]_{B_C}$ où $B_C$ est la base canonique de $\Kset_3[X]$.
        $$[f]_B = \left(\begin{matrix}
        3 & 0 & -2 & 0 \\
        0 & 4 & 0 & -6 \\
        0 & 0 & 7 & 0 \\
        0 & 0 & 0 & 12 \\
        \end{matrix}\right)$$
        Sachant que $f(1) = 3$, $f(X) = 4X$, $f(X^2) = 7X^2 - 2$ et $f(X^3) = 12X^3 - 6X$.
        \1 Soient $a_1,\dots,a_n \in \Kset$. On pose $\varphi : \begin{matrix} \Kset_{n-1}[X] &\to &\Kset^n \\ P &\mapsto &(P(a_1),\dots,P(a_n)) \end{matrix}$.\\On souhaite déterminer $\Mat(\varphi,B_{C_1},B_{C_2})$ où $B_{C_1}$ et $B_{C_2}$ sont les bases canoniques de $\Kset_{n-1}[X]$ et $\Kset^n$ respectivement.
        $$\Mat(\varphi,B_{C_1},B_{C_2}) = \left(\begin{matrix}
        1 & a_1 & \cdots & a_1^{n-1}\\
        1 & a_2 & \cdots & a_2^{n-1}\\
        \vdots & \vdots & \vdots & \vdots \\
        1 & a_n & \cdots & a_n^{n-1}
        \end{matrix}\right)$$
        Cette matrice est égale à la matrice de Vandermonde\index{Vandermonde (Matrice de)} $V(a_1,\dots,a_n)$.
    \end{outline}
\end{example}

\chapter{Opérations sur les matrices}

\begin{definition}[Espace vectoriel de matrices]
    On munit $\mathcal M_{n,p}(\Kset)$ de la LCI + et de la LCE $\cdot$ définies par :
    
    $$\forall A = (a_{i,j})_{\substack{1 \le i \le n \\ 1 \le j \le p}}, B = (b_{i,j})_{\substack{1 \le i \le n \\ 1 \le j \le p}} \in \mathcal M_{n,p}(\Kset), \forall \lambda \in \Kset :$$

    $$\begin{cases}
    A + B = (a_{i,j} + b_{i,j})_{\substack{1 \le i \le n \\ 1 \le j \le p}}\\ \lambda \cdot  A = (\lambda a_{i,j})_{\substack{1 \le i \le n \\ 1 \le j \le p}}
    \end{cases}$$

    En plus, $\mathcal M_{n,p}(\Kset)$ est de dimension finie, avec $\underset\Kset\dim(\mathcal M_{n,p}(\Kset)) = n\cdot p$.

    La base canonique de $\mathcal M_{n,p}(\Kset)$ est $B_C = (E_{i,j}(n,p))_{\substack{1 \le i \le n\\ 1 \le j \le p}}$.\index{Élémentaire (Matrice)}
\end{definition}

\begin{proof}
    Pour montrer que $\mathcal M_{n,p}(\Kset)$ est un $\Kset$-ev., il faut montrer que :
    \begin{outline}
        \1 $(\mathcal M_{n,p}(\Kset),+)$ est un groupe abélien.
        \2 ... et donc que $+$ est bien une LCI\dots
        \2 qu'elle est associative et commutative...
        \2 que $0_{n,p} = (0)_{\substack{1 \le i \le n \\ 1 \le j \le p}}$ est élément neutre pour la loi + dans $\mathcal M_{n,p}(\Kset)$.
        \2 Enfin, que les éléments de $\mathcal M_{n,p}(\Kset)$ sont symétrisables par loi +.
        \1 Ensuite, $\cdot$ est bien une LCE et qu'elles vérifient les propriétés d'une LCE pour un espace vectoriel comme énoncées dans le chapitre 14-A en page 3.
    \end{outline}
\end{proof}
\todo{recop}

\begin{theorem}[Isomorphisme entre $\mathcal L(E,F)$ et $\mathcal M_{n,p}(\Kset)$]
    Soient $E,F$ deux $\Kset$-ev. tels que $\begin{cases}
    \underset\Kset\dim E = p\\ \underset\Kset\dim F = n
    \end{cases}$

    Soient $\begin{cases}
    B = (e_1,\dots,e_p) \text{ base de } E\\
    C = (c_1,\dots,c_n) \text{ base de } F
    \end{cases}$

    Alors $\psi : \begin{matrix}
    \mathcal{L}(E,F) &\to &\mathcal M_{n,p}(\Kset)\\
    f &\mapsto & \Mat(f,B,C)
    \end{matrix}$ est un isomorphisme de $\Kset$-ev.
\end{theorem}

\begin{methodbox}
    Pour démontrer la surjectivité : ``\textit{Une application linéaire de $E$ vers $F$ est caractérisée par les images de la base de $E$}''.
\end{methodbox}

\begin{proof}
    Pour montrer que $\psi$ est un isomorphisme de $E$ vers $F$, il faut vérifier que :
    \begin{itemize}
        \item $\psi$ est bien définie.
        \item $\psi$ est une application linéaire.
        \item $\psi$ est injective.
        \item $\psi$ est surjective.
    \end{itemize}

    \textbf{$\psi$ est bien définie :}

    \begin{itemize}
        \item On a $\forall f \in \mathcal L(E,F)$.
        \item On a $\Mat(f,B,C) = \psi(f) \in \mathcal M_{n,p}(\Kset)$.
        \item Donc $\psi$ est définie.
    \end{itemize}

    \textbf{$\psi$ est une application linéaire :}

    \begin{outline}
    \1 On pose $N = \Mat(f,B,C) = (a_{i,j})_{\substack{1 \le i \le n\\ 1 \le j \le p}}$.
    \2 On a donc $N = \left(\begin{matrix} f(e_1) & \cdots & f(e_p)\\
    a_{1,1} & \cdots & a_{1,p} \\
    \vdots & & \vdots \\
    a_{n,1} & \cdots & a_{n,p}
    \end{matrix}\right)$.
    \1 On pose $M = \Mat(g,B,C) = (b_{i,j})_{\substack{1 \le i \le n\\ 1 \le j \le p}}$.
    \2 Donc $\forall j \in [\![1,p]\!], g(e_j) = \sum_{i=1}^{n}b_{i,j}c_i$.
    \1 Donc $N + \lambda M = (a_{i,j} + b_{i,j})_{\substack{1 \le i \le n\\ 1 \le j \le p}}$.
    \1 Or $\forall j \in [\![1,p]\!], (f+\lambda g)(e_j)$.
    \begin{align*}
        (f+ \lambda g)(e_j) &= f(e_j) + \lambda g(e_j)\\
        &= \sum_{i=1}^n a_{i,j} c_i + \lambda \sum_{i=1}^{n}b_{i,j}c_i\\
        &= \sum_{i=1}^{n}(a_{i,j}+\lambda b_{i,j})\cdot c_i
    \end{align*}
    \1 Cela implique que $\underbrace{\Mat(f+\lambda g,B,C)}_{=\psi(f+\lambda g)}= \underbrace{N}_{\psi(g)} + \lambda \underbrace{M}_{\psi(g)}$.
    \1 Donc $\psi \in \mathcal L(\mathcal L(E,F), \mathcal M_{n,p}(\Kset))$.
    \end{outline}

    \textbf{$\psi$ est injective :}

    \begin{outline}
    \1 Soit $f \in \Ker(\psi)$. Donc $\psi(f) = 0_{n,p}$.
    \2 i.e. : (matrices en latex ehhh)
    \1 Donc $\forall 1 \le j \le p, f(e_j) = 0_F$.
    \1 Or $f(E) = \Vect(f(B)) = \{0_F\}$.
    \1 Donc $f = 0_{\mathcal L(E,F)}$.
    \1 i.e. $\Ker\psi = 0_{\mathcal L(E,F)}$.
    \1 Et donc $\psi$ est injective !
    \end{outline}

    \textbf{$\psi$ est surjective :}

    \begin{outline}
        \1 Soit $A = (a_{i,j})_{\substack{1 \le i \le n\\ 1 \le j \le p}} \in \mathcal M_{n,p}(\Kset)$.
        \1 On pose $f : \begin{matrix} E &\to &F \\ e_j &\mapsto &\sum_{i=1}^n a_{i,j} c_i \end{matrix}$ pour tout $1 \le j \le p$.
        \1 $f$ est bien définie, car $B$ est une base de $E$.
        \1 $f$ est linéaire, car $C$ est une base de $F$.
        \1 Donc $f \in \mathcal L(E,F)$.
        \1 Or $\psi(f) = \Mat(f,B,C) = A$.
        \1 Donc $\psi$ est surjective !
    \end{outline}

    On a montré que $\psi$ est un isomorphisme de $\Kset$-ev. entre $\mathcal L(E,F)$ et $\mathcal M_{n,p}(\Kset)$.
\end{proof}
\todo{ECRIRE MATRICES}

% khollen spetzialen

\begin{consequence}
    Soient $E,F$ deux $\Kset$-ev. de dimensions finies.

    $\mathcal L(E,F)$ est aussi de dimension finie, avec $\underset\Kset\dim(\mathcal L(E,F)) = \underset\Kset\dim(E) \cdot \underset\Kset\dim(F)$.
\end{consequence}

\begin{proof}
    En effet, si $\underset\Kset\dim(E) = p$ et $\underset\Kset\dim(F) = n$, alors $\mathcal L(E,F)$ est isomorphe à $\mathcal M_{n,p}(\Kset)$, qui est de dimension $n \cdot p$.

    $$\mathcal L(E,F) \sim \mathcal M_{n,p}(\Kset) \implies \underset\Kset\dim(\mathcal L(E,F)) = \underset\Kset\dim(\mathcal M_{n,p}(\Kset)) = n \cdot p$$
\end{proof}

\begin{remark}
    Si $E,F$ sont deux $\Kset$-ev. de dimensions finies tels que $E \sim F$, alors $\underset\Kset\dim(E) = \underset\Kset\dim(F)$.
\end{remark}

\begin{property}
    Soit $E$ un $\Kset$-ev. de dimension $n \in \N^*$.
    $$E \sim \Kset^n$$
\end{property}

\begin{proof}
    Soit $C = (c_1,\dots,c_n)$ une base de $E$.

    On pose $B_C = (e_1,\dots,e_n)$ la base canonique de $\Kset^n$.

    On définit $\varphi : \begin{matrix} K^n &\to &E \\ (\lambda_1,\dots,\lambda_n) &\mapsto &\sum_{i=1}^n \lambda_i c_i \end{matrix}$.

    \begin{outline}
        \1 $\varphi$ est bien définie, car $C$ est une base de $E$.
        \1 $\varphi$ est linéaire, car $C$ est une base de $E$.
        \1 Donc $\varphi \in \mathcal L(\Kset^n,E)$.
        \1 On a $\varphi(B_C) = C$, qui est une base de $E$.
        \1 Donc $\varphi$ est un isomorphisme de $\Kset$-ev. entre $\Kset^n$ et $E$.
    \end{outline}
\end{proof}

\begin{consequence}
    Soit $E,F$ deux $\Kset$-ev. de dimensions finies.

    $$E \sim F \iff \underset\Kset\dim(E) = \underset\Kset\dim(F)$$
\end{consequence}

\begin{definition}[Application linéaire associée canoniquement à une matrice]
    Soit $A = (a_{i,j})_{\substack{1 \le i \le n \\ 1 \le j \le p}} \in \mathcal M_{n,p}(\Kset)$.

    L'application $f \in \mathcal L(\Kset^p,\Kset^n)$ telle que $\Mat(f,B_{C_1},B_{C_2}) = A$ est appelée l'application linéaire associée canoniquement à $A$.

    (Avec $B_{C_1}=B_C^{(p)}$ et $B_{C_2}=B_C^{(n)}$ les bases canoniques de $\Kset^p$ et $\Kset^n$ respectivement.)
\end{definition}

\begin{example}
    \begin{itemize}
        \item Soit $A = \left(\begin{matrix}
        1 & 2 & 3
        \\
        4 & 5 & 6
        \end{matrix}\right)$. L'application linéaire associée canoniquement à $A$ est $f : \begin{matrix} \Kset^3 &\to &\Kset^2 \\ (\lambda_1,\lambda_2,\lambda_3) &\mapsto &(1\cdot \lambda_1 + 2\cdot \lambda_2 + 3\cdot \lambda_3, 4\cdot \lambda_1 + 5\cdot \lambda_2 + 6\cdot \lambda_3) \end{matrix}$.
        \item Soit $A = \left(\begin{matrix}
        -1 & 1 & 0 \\
        2 & 3 & -2
        \end{matrix}\right) \in \mathcal M_{2,3}(\R)$.
    
        Pour déterminer l'application linéaire associée canoniquement à $A$, on pose $x,y,z \in \R$.

        \begin{align*}
            f(x,y,z) &= f(x\cdot e_1 + y \cdot e_2 + z \cdot e_3)\\
            &= x \cdot f(e_1) + y \cdot f(e_2) + z \cdot f(e_3)\\
            &= x \cdot (-1,2) + y \cdot (1,3) + z \cdot (0,-2)\\
            &= (-x+y, 2x+3y-2z)
        \end{align*}

            Donc $f : \begin{matrix} \R^3 &\to &\R^2 \\ (x,y,z) &\mapsto &(-x+y, 2x+3y-2z) \end{matrix}$ est l'application linéaire associée canoniquement à $A$.
    \end{itemize}
\end{example}

\begin{theorem}[Lemme]
    Pour tout $A,B \in \mathcal M_{n,p}(\Kset)$ et pour tout $\lambda \in \Kset$, on a :
    $$^t(A+\lambda B) = \,{^t}A + \lambda \cdot ^tB$$
    \label{th:transposée}
\end{theorem}

\begin{proof}
    On pose $A = (a_{i,j})_{\substack{1 \le i \le n \\ 1 \le j \le p}}$ et $B = (b_{i,j})_{\substack{1 \le i \le n \\ 1 \le j \le p}}$.
    
    On pose aussi $^tA = (c_{i,j})_{\substack{1 \le i \le p \\ 1 \le j \le n}}$ et $^tB = (d_{i,j})_{\substack{1 \le i \le p \\ 1 \le j \le n}}$.
    
    On pose aussi $(A+\lambda B) = (e_{i,j})_{\substack{1 \le i \le n \\ 1 \le j \le p}}$ et $^t(A+\lambda B) = (f_{i,j})_{\substack{1 \le i \le p \\ 1 \le j \le n}}$.

    On a, $\forall 1 \le i \le p, \forall 1 \le j \le n$ :
    \begin{align*}
        f_{i,j} &= e_{j,i}\\
        &= a_{j,i} + \lambda b_{j,i}\\
        &= c_{i,j} + \lambda d_{i,j}
    \end{align*}

    Donc $^t(A+\lambda B) = \,{^t}A + \lambda \cdot ^tB$.
\end{proof}

\begin{property}
    \begin{enumerate}
        \item $T_{n,s}(\Kset)$ et $T_{n,i}(\Kset)$ sont des sous-espaces vectoriels supplémentaires isomorphes de $\mathcal M_n(\Kset)$ avec $\underset\Kset\dim(T_{n,s}(\Kset)) = \underset\Kset\dim(T_{n,i}(\Kset)) = \frac{n(n+1)}{2}$.
        \item $S_n(\Kset)$ et $A_n(\Kset)$ sont des sous-espaces vectoriels de $\mathcal M_n(\Kset)$ avec $\underset\Kset\dim(S_n(\Kset)) = \frac{n(n+1)}{2}$ et $\underset\Kset\dim(A_n(\Kset)) = \frac{n(n-1)}{2}$.
        \item $D_n(\Kset)$ est un sous-espace vectoriel de $\mathcal M_n(\Kset)$ avec $\underset\Kset\dim(D_n(\Kset)) = n$.
    \end{enumerate}
\end{property}

\begin{proof}
    \begin{outline}
        \1 Première propriété :
            \2 $T_{n,s}(\Kset)$ et $T_{n,i}(\Kset)$ sont des sous-espaces vectoriels de $\mathcal M_n(\Kset)$ (trivial).
            \2 Pour montrer qu'ils sont isomorphes, on pose $T: \begin{matrix} T_{n,s}(\Kset) &\to &T_{n,i}(\Kset) \\ A &\mapsto &^tA \end{matrix}$.
            \3 $T$ est bien définie (trivial).
            \3 D'après le lemme \ref{th:transposée}, $T$ est linéaire.
            \3 Et $\forall B \in T_{n,i}(\Kset), \exists ! A \in T_{n,s}(\Kset), B = \,^tA$. Donc $T$ est bijective.
            \2 Donc $T$ est un isomorphisme de $\Kset$-ev. entre $T_{n,s}(\Kset)$ et $T_{n,i}(\Kset)$.
            \2 Ils ont donc la même dimension.
            \2 Pour déterminer leur dimension, on pose $A = (a_{i,j})_{1 \le i,j \le n} \in T_{n,s}(\Kset)$.
            \2 On sait que $\forall i > j, a_{i,j} = 0$.
            \2 Donc $A = \sum_{1 \le i,j \le n} a_{i,j} E_{i,j}(n) = \sum_{1 \le i \le j \le n} a_{i,j} E_{i,j}(n)$.
            \2 Donc $B = (E_{i,j}(n))_{1 \le i \le j \le n}$ est une base de $T_{n,s}(\Kset)$.
            \2 Donc $\underset\Kset\dim(T_{n,s}(\Kset)) = \card{B} = \frac{n(n+1)}{2}$ (il s'agit d'une somme de $1 + 2 + \cdots + n$).
            \2 Donc $\underset\Kset\dim(T_{n,i}(\Kset)) = \frac{n(n+1)}{2}$.
            \2 \textit{Note additionnelle :} Une base de $T_{n,i}(\Kset)$ est $B' = (E_{i,j}(n))_{1 \le j \le i \le n}$ (on permute les indices $i$ et $j$).
        \1 Deuxième propriété :
            \2 $S_n(\Kset)$ et $A_n(\Kset)$ sont des sous-espaces vectoriels de $\mathcal M_n(\Kset)$ (trivial).
            \2 Pour montrer que $S_n(\Kset)$ et $A_n(\Kset)$ sont supplémentaires dans $\mathcal M_n(\Kset)$, on vérifie que $S_n(\Kset) \cap A_n(\Kset) = \{0_{\mathcal M_n(\Kset)}\}$ et que $S_n(\Kset) + A_n(\Kset) = \mathcal M_n(\Kset)$.
            \3 Soit $A \in S_n(\Kset) \cap A_n(\Kset)$. Donc $^tA = A$ et $^tA = -A$.
            \3 Donc $A = -A$, donc $2A = 0_{\mathcal M_n(\Kset)}$.
            \3 Donc $A = 0_{\mathcal M_n(\Kset)}$
            \3 Donc $S_n(\Kset) \cap A_n(\Kset) = \{0_{\mathcal M_n(\Kset)}\}$.
            \3 Soit $A = (a_{i,j})_{1 \le i,j \le n} \in \mathcal M_n(\Kset)$.
            \3 On pose $B = \frac{1}{2}(A + \,^tA)$ et $C = \frac{1}{2}(A - \,^tA)$.
            \3 Donc $^tB = \frac{1}{2}(^tA + A) = B$ et $^tC = \frac{1}{2}(^tA - A) = -C$.
            \3 Donc $B \in S_n(\Kset)$ et $C \in A_n(\Kset)$.
            \3 Donc $A = B + C \in S_n(\Kset) + A_n(\Kset)$.
            \3 Donc $S_n(\Kset) + A_n(\Kset) = \mathcal M_n(\Kset)$. (Dans le cours, on a procédé par analyse-synthèse).
            \2 Donc $S_n(\Kset)$ et $A_n(\Kset)$ sont des sous-espaces vectoriels supplémentaires de $\mathcal M_n(\Kset)$.
            \2 Pour déterminer leur dimension, on pose $A = (a_{i,j})_{1 \le i,j \le n} \in S_n(\Kset)$.
            \2 On va construire une base de $S_n(\Kset)$ à partir de la base canonique de $\mathcal M_n(\Kset)$.
            \3 On sait que $\forall 1 \le i,j \le n, a_{i,j} = a_{j,i}$.
            \3 Donc $A = \sum_{1 \le i,j \le n} a_{i,j} E_{i,j} = \sum_{1 \le i \le j \le n} a_{i,j} E_{i,j} + \sum_{1 \le j < i \le n} a_{i,j} E_{i,j}$.
            \3 Or $\forall 1 \le j < i \le n, a_{i,j} = a_{j,i}$.
            \3 Donc $A = \sum_{i=1}^n a_{i,i} E_{i,i} + \sum_{1 \le i < j \le n} a_{i,j} (E_{i,j} + E_{j,i})$.
            \3 Donc $B = (E_{i,i}(n))_{1 \le i \le n} \cup (E_{i,j}(n) + E_{j,i}(n))_{1 \le i < j \le n}$ est une base de $S_n(\Kset)$.
            \2 Donc $\underset\Kset\dim(S_n(\Kset)) = \card{B} = n + \frac{n(n-1)}{2} = \frac{n(n+1)}{2}$.
            \2 Donc $\underset\Kset\dim(A_n(\Kset)) = \frac{n(n-1)}{2}$.
            \3 On peut aussi déterminer une base de $A_n(\Kset)$ à partir de la base de $S_n(\Kset)$.
            \3 En effet, on sait que $B = (E_{i,i}(n))_{1 \le i \le n} \cup (E_{i,j}(n) + E_{j,i}(n))_{1 \le i < j \le n}$ est une base de $S_n(\Kset)$.
            \3 Donc $B' = (E_{i,j}(n) - E_{j,i}(n))_{1 \le i < j \le n}$ est une base de $A_n(\Kset)$ (on permute les signes dans $\sum_{1 \le i < j \le n} a_{i,j} (E_{i,j} + E_{j,i})$ parce que $a_{i,j} = -a_{j,i}$ et on enlève les éléments de la forme $a_{i,i} E_{i,i}$ parce que $a_{i,i} = 0$).
        \1 Troisième propriété :
            \2 Méthode 1 : Réutiliser la première propriété et le fait que $D_n(\Kset) = T_{n,s}(\Kset) \cap T_{n,i}(\Kset)$.
            \2 Méthode 2 : $D_n(\Kset) = \Vect(E_{i,i}(n))_{1 \le i \le n}$, qui est de dimension $n$.
    \end{outline}
\end{proof}

\begin{methodbox}
    Quelques résultats intéressants ont été trouvés lors de la démonstration :
    \begin{itemize}
        \item Une base de $T_{n,s}(\Kset)$ est $B = (E_{i,j}(n))_{1 \le i \le j \le n}$.
        \item Une base de $T_{n,i}(\Kset)$ est $B' = (E_{i,j}(n))_{1 \le j \le i \le n}$.
        \item Une base de $S_n(\Kset)$ est $B = (E_{i,i}(n))_{1 \le i \le n} \cup (E_{i,j}(n) + E_{j,i}(n))_{1 \le i < j \le n}$.
        \item Une base de $A_n(\Kset)$ est $B' = (E_{i,j}(n) - E_{j,i}(n))_{1 \le i < j \le n}$.
    \end{itemize}
\end{methodbox}

\begin{property}
    Soient $E,F,G$ trois $\Kset$-ev. tels que :
    $$\underset\Kset\dim(E) = q, B = (e_1,\dots,e_q) \text{ base de } E$$
    $$\underset\Kset\dim(F) = p, C = (c_1,\dots,c_p) \text{ base de } F$$
    $$\underset\Kset\dim(G) = n, D = (d_1,\dots,d_n) \text{ base de } G$$

    Soient $f \in \mathcal L(E,F)$ et $g \in \mathcal L(F,G)$.

    On pose $\displaystyle\begin{cases} A_1 = \Mat(f,B,C) \in \mathcal M_{p,q}(\Kset) = (b_{i,j})_{\substack{1 \le i \le p \\ 1 \le j \le q}}\\
    A_2 = \Mat(g,C,D) \in \mathcal M_{n,p} = (a_{i,j})_{\substack{1 \le i \le n \\ 1 \le j \le p}}
    \end{cases}$

    Alors $\Mat(g \circ f, B, D) = (\alpha_{i,j})_{\substack{1 \le i \le n \\ 1 \le j \le q}}$ tels que $\alpha_{i,j} = \sum_{k=1}^p a_{i,k} b_{k,j}$.
\end{property}

\begin{proof}
\begin{itemize}
    \item On pose $A = \Mat(g \circ f, B, D) = (\alpha_{i,j})_{\substack{1 \le i \le n \\ 1 \le j \le q}}$.

    \item On a $\forall j \in [\![1,q]\!], (g \circ f)(e_j) = \sum_{i=1}^n \alpha_{i,j} d_i$.

    \item Or $\forall j \in [\![1,q]\!], f(e_j) = \sum_{k=1}^p b_{k,j} c_k$.

    \item Donc $\forall j \in [\![1,q]\!], (g \circ f)(e_j) = g(f(e_j)) = g\left(\sum_{k=1}^p b_{k,j} c_k\right) = \sum_{k=1}^p b_{k,j} g(c_k)$.

    \item Or $\forall k \in [\![1,p]\!], g(c_k) = \sum_{i=1}^n a_{i,k} d_i$.

    \item Donc $\forall j \in [\![1,q]\!], (g \circ f)(e_j) = \sum_{k=1}^p b_{k,j} g(c_k) = \sum_{k=1}^p b_{k,j} \left(\sum_{i=1}^n a_{i,k} d_i\right) = \sum_{i=1}^n \left(\sum_{k=1}^p a_{i,k} b_{k,j}\right) d_i$.

    \item Donc $\forall j \in [\![1,q]\!], (g \circ f)(e_j) = \sum_{i=1}^n \left(\sum_{k=1}^p a_{i,k} b_{k,j}\right) d_i$.

    \item Donc $A = (\alpha_{i,j})_{\substack{1 \le i \le n \\ 1 \le j \le q}}$ tels que $\alpha_{i,j} = \sum_{k=1}^p a_{i,k} b_{k,j}$.
\end{itemize}
\end{proof}

\begin{definition}[Produit de matrices]
    Soit $A = (a_{i,j})_{\substack{1 \le i \le n \\ 1 \le j \le p}} \in \mathcal M_{n,p}(\Kset)$.
    Soit $B = (b_{i,j})_{\substack{1 \le i \le p \\ 1 \le j \le q}} \in \mathcal M_{p,q}(\Kset)$.
    
    On appelle produit de $A$ par $B$ la matrice notée :
    
    $$AB = A\times B = (c_{i,j})_{\substack{1 \le i \le n \\ 1 \le j \le q}} \in \mathcal M_{n,q}(\Kset)$$
    
    tels que : 
    $$\forall 1 \le i \le n, \forall 1 \le j \le q, c_{i,j} = \sum_{k=1}^p a_{i,k} b_{k,j}$$
\end{definition}

\begin{attention}
    Le produit de deux matrices n'est défini que si le nombre de colonnes de la première matrice est égal au nombre de lignes de la deuxième matrice.
\end{attention}

\begin{figure}[H]
    % Styles TikZ pour le diagramme
\tikzset{
    % Style pour les éléments mis en évidence (blocs roses)
    highlighted-element/.style={
        draw=none,
        fill=pink!50,
        inner sep=3pt,
        rounded corners=1pt
    },
    % Style pour les flèches d'interaction de multiplication
    connect-arrow/.style={
        -Stealth,
        red!80!black,
        thick
    },
    % Style pour les flèches d'étiquette en pointillé
    dotted-arrow/.style={
        densely dotted,
        -Stealth,
        blue!70!black
    },
    % Style pour les étiquettes i et j
    label-text/.style={
        font=\large,
        blue!70!black
    }
}

\begin{tikzpicture}[>=Stealth]

    % --- MATRICE A (Gauche, Ligne i) ---
    \matrix (A) [
        matrix of math nodes,
        left delimiter=(,
        right delimiter=),
        row sep=0.5cm,
        column sep=0.2cm,
        nodes={anchor=center}
    ]
    {
        \dots & \dots & \dots & & \dots & \dots & \dots \\
        |[highlighted-element]| a_{i,1} & |[highlighted-element]| a_{i,2} & |[highlighted-element]| a_{i,3} & \cdots & |[highlighted-element]| a_{i,k} & |[highlighted-element]| a_{i,p-1} & |[highlighted-element]| a_{i,p} \\
        \dots & \dots & \dots & & \dots & \dots & \dots \\
    };

    % Étiquette de ligne 'i' à gauche
    \node (ilabel) [left=1.5cm of A-2-1.west, label-text] {i};
    \draw[dotted-arrow] (ilabel) -- (A-2-1.west);

    % Dimensions de la Matrice A
    \node [below right=0.2cm and 0.1cm of A.south east, font=\scriptsize, cyan!70!black] {(n,p)};

    % --- SYMBOLE MULTIPLICATION ---
    \node (times) [right=1cm of A.east, scale=1.5] {$\times$};

    % --- MATRICE B (Droite, Colonne j) ---
    \matrix (B) [
        matrix of math nodes,
        left delimiter=(,
        right delimiter=),
        row sep=0.2cm,
        column sep=0.5cm,
        right=1cm of times,
        nodes={anchor=center}
    ]
    {
        \vdots & |[highlighted-element]| b_{1,j} & \vdots \\
        \vdots & |[highlighted-element]| b_{2,j} & \vdots \\
        \vdots & |[highlighted-element]| b_{3,j} & \vdots \\
        & \vdots & \\
        \vdots & |[highlighted-element]| b_{k,j} & \vdots \\
        & \vdots & \\
        \vdots & |[highlighted-element]| b_{p-1,j} & \vdots \\
        \vdots & |[highlighted-element]| b_{p,j} & \vdots \\
    };

    % Étiquette de colonne 'j' en haut
    \node (jlabel) [above=1cm of B-1-2.north, label-text] {j};
    \draw[dotted-arrow] (jlabel) -- (B-1-2.north);

    % Dimensions de la Matrice B
    \node [below right=0.2cm and 0.1cm of B.south east, font=\scriptsize, cyan!70!black] {(p,q)};

    % --- FLÈCHES D'INTERACTION DE MULTIPLICATION ---
    % Relier les éléments correspondants de A et B
    \draw[connect-arrow] (A-2-1.north) to[out=45, in=135] (B-1-2.north west);
    \draw[connect-arrow] (A-2-3.north) to[out=30, in=150] (B-3-2.north west);
    \draw[connect-arrow] (A-2-5.north) to[out=25, in=155] (B-5-2.north west);

\end{tikzpicture}
    \caption{Illustration du produit de matrices $AB$ avec les éléments $a_{i,k}$ et $b_{k,j}$ mis en évidence.}
\end{figure}

\begin{consequence}
    Avec les hypothèses de la propriété précédente, on a :
    $$\Mat(g \circ f, B, D) = \Mat(g,C,D) \times \Mat(f,B,C)$$
\end{consequence}

\begin{consequence}
    \begin{enumerate}
        \item Soient $E$ un $\Kset$-ev. de dimension $n$ et $B$ une base de $E$. Soient $f,g \in \mathcal L(E)$ alors $[g\circ f]_B = [g]_B \times [f]_B$.
        \item Soient $E$ un $\Kset$-ev. de dimension $n$ et $B$ une base de $E$. Soit $f \in \mathcal L(E)$ alors $[f^k]_B = [f]_B^k$ pour tout $k \in \N^*$. \textit{Cela peut être utile pour déterminer le polynôme annulateur\index{Polynôme annulateur} de l'application $f$.}
    \end{enumerate}
\end{consequence}

\begin{example}
    \begin{enumerate}
        \item $\left(\begin{matrix} 1 & -2 & 0 \\ -1 & 3 & 2 \end{matrix}\right) \times \left(\begin{matrix} 4 & 1 \\ -1 & -2 \\ 2 & 0 \end{matrix}\right) = \left(\begin{matrix} 1\cdot 4 + (-2)\cdot (-1) + 0\cdot 2 & 1\cdot 1 + (-2)\cdot (-2) + 0\cdot 0 \\ -1\cdot 4 + 3\cdot (-1) + 2\cdot 2 & -1\cdot 1 + 3\cdot (-2) + 2\cdot 0 \end{matrix}\right) = \left(\begin{matrix} 6 & 5 \\ -3 & -7 \end{matrix}\right)$.
    \end{enumerate}
\end{example}

\begin{exercise}
    Soit $\theta \in \R$. On pose :
    $$R_\theta = \left(\begin{matrix} \cos\theta & \sin\theta \\ -\sin\theta & \cos\theta \end{matrix}\right)$$

    \begin{enumerate}
        \item Calculer $R_\theta \times R_\theta'$ pour tout $\theta' \in \R$.
        \item Calculer $R_\theta^k$ pour tout $k \in \N^*$.
    \end{enumerate}

    \textbf{Solution :}
    \begin{enumerate}
        \item Après calculs, on trouve que $R_\theta \times R_\theta' = R_{\theta + \theta'}$.
        \item Par récurrence, on trouve que $R_\theta^k = R_{k\theta}$ pour tout $k \in \N^*$.
    \end{enumerate}

    \textit{On remarque aussi que $R_{-\theta} = \,^tR_\theta$.}
\end{exercise}

\begin{definition}[Matrice identité]
    Pour $n \in \N^*$, on pose :
    $$I_n = (\delta_{i,j})_{1 \le i,j \le n} \in \mathcal M_n(\Kset)$$
    (Il s'agit d'une matrice diagonale où les éléments de la diagonale sont égaux à $1$ et les autres éléments sont égaux à $0$.)

    Si $E$ est un $\Kset$-ev. de dimension $n$ et $B=(e_1,\dots,e_n)$ une base de $E$, alors l'application linéaire associée canoniquement à $I_n$ est appelée l'application identité de $E$ et est notée $\Id_E$.

    $$[\Id_E]_B = I_n$$
\end{definition}

\begin{property}
    Soit $n \ge 2$. $\mathcal M_n(\Kset)$ muni de $+$ et $\times$ (produit matriciel) est un anneau non commutatif. L'élément neutre de $\times$ est $I_n$.
\end{property}

\begin{proof}
    \begin{outline}
        \1 On sait que $(\mathcal M_n(\Kset),+,\cdot)$ est un $\Kset$-ev. 
        \1 Donc $(\mathcal M_n(\Kset),+)$ est un groupe abélien.
        \1 Soient $A,B,C \in \mathcal M_n(\Kset)$ et $a,b,c$ les endomorphismes associés canoniquement à $A,B,C$ respectivement.
        $$[a]_{B_C} = A,~~[b]_{B_C} = B,~~[c]_{B_C} = C$$
        \1 On a $A \times (B \times C) = A \times [b\circ c]_{B_C} = [a \circ (b \circ c)]_{B_C}$.
        \1 Comme $\circ$ est associative, on a $[a \circ (b \circ c)]_{B_C} = [(a \circ b) \circ c]_{B_C} = [(a \circ b)]_{B_C} \times C = (A \times B) \times C$.
        \1 Donc $\times$ est associative.
        \1 On a $A\times(B+C) = [a]_{B_C} \times [b+c]_{B_C} = [a \circ (b+c)]_{B_C} = [a\circ b + a \circ c]_{B_C} = [a\circ b]_{B_C} + [a \circ c]_{B_C} = A\times B + A \times C$.
        \2 On peut séparer $[a \circ b + a \circ c]_{B_C}$ en $[a\circ b]_{B_C} + [a \circ c]_{B_C}$ en se référant à $\psi$, l'application considérée précédemment dans la section sur les applications linéaires entre espaces de matrices, qui est un isomorphisme de $\Kset$-ev. entre $\mathcal L(E,F)$ et $\mathcal M_{n,p}(\Kset)$.
        \1 Donc $\times$ est distributive par rapport à $+$.
        \1 Soit $A = (a_{i,j})_{1 \le i,j \le n} \in \mathcal M_n(\Kset)$. On pose :
        $$\left|~\begin{matrix}
        A\times I_n = (\alpha_{i,j})_{1 \le i,j \le n} \\
        I_n \times A = (\beta_{i,j})_{1 \le i,j \le n}
        \end{matrix}\right.$$
        \1 On a $\forall 1 \le i,j \le n, \alpha_{i,j} = \sum_{k=1}^n a_{i,k} \delta_{k,j} = a_{i,j}$.
        \1 Et $\forall 1 \le i,j \le n, \beta_{i,j} = \sum_{k=1}^n \delta_{i,k} a_{k,j} = a_{i,j}$.
        \1 Donc $A\times I_n = A$ et $I_n \times A = A$.
        \1 Donc $I_n$ est l'élément neutre de $\times$.
    \end{outline}
    À ce stade-là, on a montré que $(\mathcal M_n(\Kset),+,\times)$ est un anneau. Il reste à montrer que $\times$ n'est pas commutatif. On va procéder par contre-exemple, qui seront plus du domaine des propriétés que de la preuve.
\end{proof}

\begin{property}
    Soient $p,q,r,s \in [\![1,n]\!]$ des indices.

    On pose $A = E_{p,q}$ et $B = E_{r,s}$.\index{Élémentaire (Matrice)}.

    Pour expliciter, on aura :
    $$\begin{cases}
    A = E_{p,q} = (\underbrace{\delta_{i,p}\delta_{j,q}}_{a_{i,j}})_{1 \le i,j \le n}\\
    B = E_{r,s} = (\underbrace{\delta_{i,r}\delta_{j,s}}_{b_{i,j}})_{1 \le i,j \le n}
    \end{cases}$$

    On pose :
    
    $$\begin{cases}A\times B = (c_{i,j})_{1 \le i,j \le n}
    \\
    B\times A = (d_{i,j})_{1 \le i,j \le n}\end{cases}$$

    On a $\forall 1 \le i,j \le n$ :
    
    $$c_{i,j} = \sum_{k=1}^n a_{i,k} b_{k,j} = \sum_{k=1}^n \delta_{i,p}\delta_{k,q} \delta_{k,r}\delta_{j,s}$$
    
    $$\implies \boxed{c_{i,j} = \delta_{i,p}\delta_{j,s}}$$

    De même, $\forall 1 \le i,j \le n$,
    
    $$d_{i,j} = \sum_{k=1}^n b_{i,k} a_{k,j} = \sum_{k=1}^n \delta_{i,r}\delta_{k,s} \delta_{k,p}\delta_{j,q}$$
    
    $$\implies \boxed{d_{i,j} = \delta_{i,r}\delta_{j,q}}$$

    Donc $A\times B = E_{p,s}$ si $q=r$ et $A\times B = 0_{\mathcal M_n(\Kset)}$ sinon.

    $$A\times B = \delta_{q,r} E_{p,s}$$

    De même, $B\times A = E_{r,q}$ si $s=p$ et $B\times A = 0_{\mathcal M_n(\Kset)}$ sinon.

    $$B\times A = \delta_{s,p} E_{r,q}$$

    Alors, si par exemple, $r \ne q$ et $p=s$, on a $A\times B = 0_{\mathcal M_n(\Kset)}$ et $B\times A = E_{r,q} \ne 0_{\mathcal M_n(\Kset)}$.

    Donc $A\times B \ne B\times A$.

    Cela montre que $\times$ n'est pas commutatif.
\end{property}

\begin{remark}
    \begin{enumerate}
        \item $\forall p,q,r,s \in [\![1,n]\!], E_{p,q} \times E_{r,s} = \delta_{q,r} E_{p,s}$.
        \item $(\mathcal M_n(\Kset),+,\times,\cdot)$ est une $\Kset$-algèbre non commutative.
    \end{enumerate}
\end{remark}

\begin{exercise}
    Soit $A \in \mathcal M_n(\Kset)$ telle que $\forall B \in \mathcal M_n(\Kset)$ $A\times B = B \times A$.

    Montrer que $\exists \lambda \in \Kset, A = \lambda I_n$.
\end{exercise}

\begin{property}
    Soient $E$ un $\Kset$-ev. de dimension $n$ et $B$ une base de $E$. Alors :
    $$\psi : \begin{matrix} (\mathcal L(E),+,\circ,\cdot) &\to &(\mathcal M_n(\Kset),+, \times, \cdot) \\ f &\mapsto &[f]_B \end{matrix}$$
    est un isomorphisme de $\Kset$-algèbres.
\end{property}

\begin{property}
    Soient $E,F$ deux $\Kset$-ev. de dimensions respectives $p$ et $n$. Soient $B=(e_1,\ldots,e_p)$ une base de $E$ et $C=(c_1,\ldots,c_n)$ une base de $F$. Soit $f \in \mathcal L(E,F)$.

    Soit $x \in E$. On pose :
    $$\begin{cases}
    X = \Mat_B(x)\\
    A = \Mat(f,B,C)\\
    Y = \Mat_C(f(x))
    \end{cases}$$

    $$\text{Alors } Y = A \times X$$
\end{property}

\begin{proof}
    \begin{itemize}
        \item On a $\exists ! \lambda_1, \dots, \lambda_p \in \Kset$ tels que $x = \sum_{i=1}^p \lambda_i e_i$.
        \item Donc $X = \Mat_B(x) = (\begin{matrix} \lambda_1 \\ \vdots \\ \lambda_p \end{matrix})$.
        \item On pose $A = \Mat(f,B,C) = (a_{i,j})_{\substack{1 \le i \le n \\ 1 \le j \le p}}$.
        \item Donc $\forall i \in [\![1,n]\!], f(e_i) = \sum_{j=1}^p a_{i,j} c_j$.
        \item Donc $f(x) = f\left(\sum_{i=1}^p \lambda_i e_i\right) = \sum_{i=1}^p \lambda_i f(e_i) = \sum_{i=1}^p \lambda_i \left(\sum_{j=1}^p a_{i,j} c_j\right) = \sum_{j=1}^p \left(\sum_{i=1}^p a_{i,j} \lambda_i\right) c_j$.
        \item Donc $Y = \Mat_C(f(x)) = (\begin{matrix} \sum_{i=1}^p a_{1,j} \lambda_i \\ \vdots \\ \sum_{i=1}^p a_{n,j} \lambda_i \end{matrix})$.
        \item Or $A \times X = \left(\begin{matrix} a_{1,1} & \cdots & a_{1,p} \\ \vdots & & \vdots \\ a_{n,1} & \cdots & a_{n,p} \end{matrix}\right) \times \left(\begin{matrix} \lambda_1 \\ \vdots \\ \lambda_p \end{matrix}\right) = \left(\begin{matrix} \sum_{i=1}^p a_{1,j} \lambda_i \\ \vdots \\ \sum_{i=1}^p a_{n,j} \lambda_i \end{matrix}\right)$.
        \item Donc $Y = A \times X$.
    \end{itemize}
\end{proof}
\todo{a corriger}

\begin{example}
    Soit $E$ un $\R$-ev. de dimension $3$.

    Soient $B$ une base de $E$ et $f\in \mathcal L(E)$ telle que $[f]_B = \left(\begin{matrix} 2 & -1 & -1 \\ 1 & 0 & 1 \\ 1 & -1 & 0 \end{matrix}\right)$.
    \begin{itemize}
        \item Déterminer $g \in \mathcal L(\R^3)$ telle que $A = [g]_{B_C}$.
        \item Montrer que $f$ est un projecteur de $E$.
    \end{itemize}
\end{example}

\printindex
\end{document}