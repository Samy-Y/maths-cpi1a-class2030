# Prépa Khôlle Spéciale

Soient $E$ un $\mathbb{K}$-ev. et $H$ un sev. de $E$ tel que $H \ne E$. Alors :

$H$ est un hyperplan de $E$ $\iff$ $\forall b \in E\setminus H, E = H \oplus \mathbb{K} \cdot b$.

---

Soient $E$ un $\mathbb{K}$-ev. et $H$ un sev. de $E$.
$H$ est un hyperplan de $E$ $\iff$ $\exists \varphi \in E^* \setminus \{0_{E^*}\}$ telle que $H = \mathrm{Ker}(\varphi)$.

i.e. : Tout hyperplan $H$ de $E$ est le noyau d'une forme linéaire non nulle de $E$.

---

Soient $A_1,A_2 \in \mathcal M_{n,p}(\mathbb{K})$.

$A_1$ éq $A_2 \iff A_1 \text{ et } A_2$ représentent la même application linéaire dans 2 couples de bases différents

$\iff \exists f \in \mathcal L(E,F), \exists B_1,B_2 \text{ bases de } E, \exists C_1,C_2 \text{ bases de } F, A_2 = \mathrm{Mat}(f,B_2,C_2) \text{ et } A_1 = \mathrm{Mat}(f,B_1,C_1)$

---

Toute matrice inversible a ses colonnes comme base de $\mathbb{K}^n$, et agit aussi comme une matrice de passage de la base canonique vers cette base de colonnes.